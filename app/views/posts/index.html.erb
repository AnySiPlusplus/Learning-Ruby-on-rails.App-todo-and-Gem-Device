<h1>Все статьи</h1>
	<% @post.each do |post| %>
	<div class="aler aler-light">
		<h3><%= post.title %></h3>
		<p><%= post.body %></p>
		<%= link_to "Читать далее", post_path(post), :class => ' btn btn-info' %>
	</div>
	<% end %> 